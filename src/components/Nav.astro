---
import { getLangFromUrl, useTranslations, useTranslatedPath } from '../i18n/utils';
const lang = getLangFromUrl(Astro.url);
const translatePath = useTranslatedPath(lang);

interface Props {
    pathname: string
}
const {pathname} = Astro.props;

---

<nav>
    <a href={translatePath('/')} class={pathname === translatePath('/') ? "text-red" : ""}>HOME</a>
    <a href={translatePath('/filosofie')} class={pathname === translatePath('/filosofie') ? "text-red" : ""}>FILOSOFIE</a>
    <a href={translatePath('/aanbod')} class={pathname === translatePath('/aanbod') ? "text-red" : ""}>AANBOD</a>
    <a href={translatePath('/hartcirkels')} class={pathname === translatePath('/hartcirkels') ? "text-red" : ""}>HARTCIRKELS</a>
    <a href={translatePath('/about')} class={pathname === translatePath('/about') ? "text-red" : ""}>OVER MIJ</a>
</nav>

<style>
    nav {
        display: flex;
        align-items: center;
        gap: 3rem;
    }
    a {
        padding: 0 0.7rem 0.5rem 0.7rem;
        display: inline-block;
        border-bottom: 1px solid white;
    }
    a:hover{
        transform: scale(1.2);
        border-color: var(--mooyo-red);
        transition: all 0.2s ease;
    }
</style>